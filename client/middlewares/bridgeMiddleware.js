import superagent from 'superagent'
import querystring from 'querystring'

import {requestBase, baseAPI} from '../app/configs'

export default function bridgeMiddleware({getState}) {
  return (next) => {
    return (action) => {


    }
  }
}
